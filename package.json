{
  "name": "adita",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./lib && swc src --config-file .swcrc -d lib && tsc --emitDeclarationOnly && prettier --write ."
  },
  "dependencies": {},
  "devDependencies": {
    "@swc/cli": "^0.3.12",
    "@swc/core": "^1.4.13",
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "@types/node": "^20.12.7",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
